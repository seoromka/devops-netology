docker build -t devops-netology .

docker run -p 8011:80 rkostin/devops-netology


3.

docker run -t -d --name c1 -v $(pwd)/data:/data centos:7
docker run -t -d --name c2 -v $(pwd)/data:/data debian:stable

DOCKER_BUILDKIT=0 docker build -t rkostin/ansible-netology:2.9.24 .

olegbukatchuk/ansible:2.9.24